b {
    $ los build
}

$ host="$(uname -a)"

// build main
build {
    $ cc *.a -o main
}

// test everything
test-all {
    $ los build
    $ "./test" --all
}

// run a specific test
test {
    $ los build
    $ "./test" --test $args
}
